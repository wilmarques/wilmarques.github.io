"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const cli_config_utils_1 = require("./cli-config-utils");
describe('replaceAppNameWithPath', () => {
    describe('when node is `application`', () => {
        describe('and appName is `app`', () => {
            it('still returns the node', () => {
                const node = 'application';
                const appName = 'app';
                const root = 'apps/app';
                expect(cli_config_utils_1.replaceAppNameWithPath(node, appName, root)).toEqual(node);
            });
        });
    });
    describe('when node is `library`', () => {
        describe('and appName is `lib`', () => {
            it('still returns the node', () => {
                const node = 'library';
                const appName = 'lib';
                const root = 'libs/lib';
                expect(cli_config_utils_1.replaceAppNameWithPath(node, appName, root)).toEqual(node);
            });
        });
    });
});
