"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const common_1 = require("./common");
describe('offsetFromRoot', () => {
    it('should work for normal paths', () => {
        const result = common_1.offsetFromRoot('apps/appname');
        expect(result).toBe('../../');
    });
    it('should work for paths with a trailing slash', () => {
        const result = common_1.offsetFromRoot('apps/appname/');
        expect(result).toBe('../../');
    });
    it('should work for deep paths', () => {
        const result = common_1.offsetFromRoot('apps/dirname/appname');
        expect(result).toBe('../../../');
    });
    it('should work for deep paths with a trailing slash', () => {
        const result = common_1.offsetFromRoot('apps/dirname/appname/');
        expect(result).toBe('../../../');
    });
});
