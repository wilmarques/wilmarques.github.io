"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const schematics_1 = require("@angular-devkit/schematics");
const testing_1 = require("@angular-devkit/schematics/testing");
const workspace_1 = require("@nrwl/workspace");
const testing_2 = require("@nrwl/workspace/testing");
const testing_3 = require("../../../utils/testing");
const update_tsconfig_1 = require("./update-tsconfig");
describe('updateTsconfig Rule', () => {
    let tree;
    let schema;
    beforeEach(() => __awaiter(void 0, void 0, void 0, function* () {
        tree = new testing_1.UnitTestTree(schematics_1.Tree.empty());
        tree = testing_2.createEmptyWorkspace(tree);
        schema = {
            projectName: 'my-lib',
            skipFormat: false,
            forceRemove: false,
        };
    }));
    it('should delete project ref from the tsconfig', () => __awaiter(void 0, void 0, void 0, function* () {
        tree = yield testing_3.runSchematic('lib', { name: 'my-lib' }, tree);
        let tsConfig = workspace_1.readJsonInTree(tree, '/tsconfig.json');
        expect(tsConfig.compilerOptions.paths).toEqual({
            '@proj/my-lib': ['libs/my-lib/src/index.ts'],
        });
        tree = (yield testing_3.callRule(update_tsconfig_1.updateTsconfig(schema), tree));
        tsConfig = workspace_1.readJsonInTree(tree, '/tsconfig.json');
        expect(tsConfig.compilerOptions.paths).toEqual({});
    }));
});
